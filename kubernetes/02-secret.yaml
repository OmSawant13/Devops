# NOTE: K8s Secrets are base64-encoded (NOT encrypted).
# In production, these are pulled from Vault (see Round 3).
# Values below are base64 of: genomex123 / genomex-super-secret

apiVersion: v1
kind: Secret
metadata:
  name: genomex-secrets
  namespace: genomex
type: Opaque
data:
  DB_PASSWORD: Z2Vub21leDEyMw==
  JWT_SECRET: Z2Vub21leC1zdXBlci1zZWNyZXQ=
